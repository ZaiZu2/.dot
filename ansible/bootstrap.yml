- name: Bootstrap development environment
  hosts: localhost
  vars:
    all_roles:
    - package_manager
    - git
    - zsh
    - fzf
    - fnm
    - gh
    - docker
    - sshfs
    - python
    - tmux
    - neovim
    - kitty
    - fonts
    - lazygit
    - uv

  tasks:
  - name: Run roles
    ansible.builtin.include_role:
      apply:
        tags:
          - "{{ roles_item }}"
      name: "{{ roles_item }}"
    loop_control:
      loop_var: roles_item
    with_items: "{{ all_roles }}"
